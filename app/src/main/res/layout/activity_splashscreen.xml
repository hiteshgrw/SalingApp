<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".splashscreen"
    android:background="#00BCD4">

    <ImageView
        android:layout_width="320dp"
        android:layout_height="210dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginTop="100dp"
        android:src="@drawable/bwicon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/roundcorners"
        android:id="@+id/ivsplashimg"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ivsplashimg"
        android:layout_marginTop="150dp"
        android:text="Narayani Book World"
        android:textSize="24sp"
        android:textColor="@android:color/holo_purple"
        android:textStyle="italic"
        android:fontFamily="sans-serif-medium"
        android:id="@+id/tvsplashsc1"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvsplashsc1"
        android:layout_marginTop="10dp"
        android:text="Books the need of nation, We the supplier of need"
        android:gravity="center"
        android:textSize="18sp"
        android:textColor="@android:color/holo_purple"
        android:textStyle="italic"
        android:fontFamily="sans-serif-medium"
        android:id="@+id/tvsplashsc2"/>

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@+id/tvsplashsc1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ivsplashimg" />

</androidx.constraintlayout.widget.ConstraintLayout>